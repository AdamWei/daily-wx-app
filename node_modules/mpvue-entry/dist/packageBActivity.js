import Vue from 'vue'
import App from '@/packageB/activity'
import store from '@/store'

Vue.config.productionTip = false
App.store = store

const app = new Vue(App)
app.$mount()

export default {
  "navigationBarTitleText": "活动页"
}
